{
  "version": 2,
  "builds": [
    { "src": "api/unlock.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/unlock.py" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
